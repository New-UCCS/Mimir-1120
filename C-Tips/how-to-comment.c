// This is a simple comment! This can be very useful for one-line comments
#include <stdio.h>

int main()
{
  // Code begins
  int foo = 1;
  printf("%i\n", foo);
  /*
  This is a large-section-comment
  Meaning I can make comments on multiple lines, and not have to include '//' every time!
  It starts with '/*' and then ends with the opposite variation.
  This is useful to comment out a large section of code!
  */
}
